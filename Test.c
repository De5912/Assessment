#include <stdio.h>
int main () {
	int a, b, c, sum, avg;
	printf("Enter the Value of a, b and c:\n");
	scanf("%d%d%d", &a, &b, &c);
	sum = a + b + c;
	avg = sum/3
	printf("The sum of a, b and c is %d", &sum);
	printf("The average is %d", &avg);
	return 0;
}
